import React from 'react';
import { Head } from '../components';

const Typed = ({ match }) => {
  return (
    <div className="home">
      <Head />
      Typed
    </div>
  );
};

export default Typed;